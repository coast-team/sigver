syntax = "proto3";

message FromOpener {
  oneof content {
    string key = 1;
    Data data = 2;
  }
}

message ToOpener {
  oneof content {
    boolean isOk = 1;
    Data data = 2;
  }
}

message Data {
  number id = 2;
  string data = 3;
}
