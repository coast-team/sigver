syntax = "proto3";

import "opener.proto";
import "joining.proto";

message IncomingMessage {
  oneof sender {
    FromOpener message = 1;
    FromJoining message = 2;
  }
}

message OutcomingMessage {
  oneof receiver {
    ToOpener message = 1;
    ToJoining message = 2;
  }
}
